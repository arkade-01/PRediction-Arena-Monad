# Monad Private Key (Deployer/Bot Wallet)
MONAD_PRIVATE_KEY="0x..."

# Network Selection (testnet | mainnet)
# Defaults to mainnet if not set
NETWORK="mainnet"

# API URL for agent data (your deployed frontend)
API_URL="https://your-app.onrender.com"

# Backend Secret (must match BACKEND_SECRET in web/.env)
BACKEND_SECRET="your-secret-here"
